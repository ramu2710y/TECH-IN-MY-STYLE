# Batch 9-10 Topics to Add

## Current Status
- **Completed**: 48/60 topics (80%)
- **Missing**: 12 topics from Batches 9-12

## Batch 9: Arrays Part 1 (5 topics)

### 1. create-arrays
- Description: Array literal [], Array(), Array.of(), Array.from()
- Interactive demo with different array creation methods
- Show literal syntax, constructor, Array.of(), Array.from()

### 2. array-access
- Description: Read elements by index and use the .length property
- Interactive demo with bracket notation
- Show positive/negative indices, length property

### 3. push-pop
- Description: Add or remove elements at the end of an array
- Interactive demo with push() and pop() methods
- Show how they modify the original array

### 4. shift-unshift
- Description: Add or remove elements at the beginning
- Interactive demo with shift() and unshift() methods
- Show how they modify the original array

### 5. splice
- Description: Insert, remove, or replace elements at any index
- Interactive demo with splice() method
- Show removing, adding, and replacing elements

## Batch 10: Arrays Part 2 (5 topics)

### 6. slice
- Description: Extract a portion of an array as a new array
- Interactive demo with slice() method
- Show start/end indices, negative indices

### 7. forEach
- Description: Iterate over every element with a callback
- Interactive demo with forEach() method
- Show callback parameters (element, index, array)

### 8. map
- Description: Transform elements and return a new array
- Interactive demo with map() method
- Show transforming numbers, strings, objects

### 9. filter
- Description: Keep only elements that pass a test function
- Interactive demo with filter() method
- Show filtering by conditions

### 10. reduce
- Description: Accumulate array values to a single output
- Interactive demo with reduce() method
- Show sum, product, finding max/min

## Format Requirements

Each topic needs:
1. **HTML**: Properly formatted with line breaks and 2-space indentation
2. **CSS**: Properly formatted with line breaks and proper selectors
3. **JS**: Interactive code with console logging
4. **Structure**: `{ topic, description, html, css, js }`

## Location
Add these topics before the closing `];` in:
`client/src/modules/js-basic-course/data/jsTopicExamples.jsx`

After line 6827 (after `higher-order-functions` topic)
